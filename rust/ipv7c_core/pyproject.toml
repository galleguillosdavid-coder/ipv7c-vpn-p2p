[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "ipv7c-core"
version = "0.3.0"
description = "Optional Rust/PyO3 acceleration core for IPv7C"
requires-python = ">=3.10"

[tool.maturin]
module-name = "ipv7c_core"
features = ["pyo3/extension-module"]
